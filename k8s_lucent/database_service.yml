apiVersion: v1
kind: Service
metadata:
  name: lucent-database-service
spec:
  type: NodePort
  selector:
    app: database-pod
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 5432