apiVersion: v1
kind: Service
metadata:
  name: lucent-frontend-service
spec:
  type: NodePort
  selector:
    app: frontend-pod
  ports:
  - protocol: TCP
    port: 5173
    targetPort: 5173
    nodePort: 80