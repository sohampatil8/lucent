apiVersion: v1
kind: Service
metadata:
  name: lucent-backend-service
spec:
  type: ClusterIP
  selector:
    app: backend-pod
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
# apiVersion: v1
# kind: Service
# metadata:
#   name: lucent-backend-service
# spec:
#   type: NodePort
#   selector:
#     app: backend-pod
#   ports:
#     - protocol: TCP
#       port: 5000
#       targetPort: 5000
#       nodePort: 30007